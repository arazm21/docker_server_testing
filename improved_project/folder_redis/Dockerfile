# Use official Redis as base image
FROM redis:7-alpine

# Optional: copy custom config (if you have one)
# COPY redis.conf /usr/local/etc/redis/redis.conf

# Optional: run Redis with custom config
# CMD ["redis-server", "/usr/local/etc/redis/redis.conf"]

# Default: run Redis with default config
CMD ["redis-server"]
